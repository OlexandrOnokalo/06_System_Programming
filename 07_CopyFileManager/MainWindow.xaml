<Window x:Class="_07_CopyFileManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_07_CopyFileManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" >
    <Window.Resources>
        <Style TargetType="{x:Type Button }">
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="Padding" Value="10"></Setter>
            <Setter Property="Background" Value="LightBlue"></Setter>
        </Style>
        <Style TargetType="{x:Type TextBox }">
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        </Style>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <Grid Margin="5" DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label FontSize="15" FontWeight="Bold">From :</Label>
            <TextBox x:Name="sourceTb" Grid.Column="1" Text="{Binding Source}"  ></TextBox>
            <Label FontSize="15" FontWeight="Bold" Grid.Row="1">To :</Label>
            <TextBox x:Name="DestTb" Grid.Row="1" Grid.Column="1"
                     Text="{Binding Destination}"></TextBox>
            <Button  Grid.Column="2" Click="SourceBtn">Browse</Button>
            <Button  Grid.Column="2" Grid.Row="1" Click="DestBtn">Browse</Button>
            <Button Grid.Row="2" Grid.ColumnSpan="3" 
              FontSize="25" FontWeight="Bold" Click="CopyBtn">Copy</Button>


        </Grid>
        <ProgressBar Name="progress" Height="15" 
                     DockPanel.Dock="Bottom" IsIndeterminate="{Binding IsWaiting}"></ProgressBar>
        <ListBox ItemsSource="{Binding Processes}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 DockPanel.Dock="Top" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ListBoxItem>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MaxWidth="100"></ColumnDefinition>
                                <ColumnDefinition Width="Auto" MaxWidth="100"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Filename}"
                                       Margin="5" TextTrimming="WordEllipsis"></TextBlock>
                            <TextBlock Grid.Column="1"  Margin="5" > 
                               <Run Text="{Binding Percentage}"></Run>  %
                            </TextBlock>


                            <ProgressBar Grid.Column="2" Value="{Binding Percentage}"></ProgressBar>
                        </Grid>


                    </ListBoxItem>
                </DataTemplate>
            </ListBox.ItemTemplate>
 
        </ListBox>

    </DockPanel>

</Window>